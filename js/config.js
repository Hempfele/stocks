// Fill these in to go live (see README.md). Leave empty to run in demo mode
// with fake players and prices — no account or network needed.
export const config = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
};

export const isDemo = !config.SUPABASE_URL;
